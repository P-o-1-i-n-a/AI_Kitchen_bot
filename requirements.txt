aiogram==3.4.1  # Фиксируем версию для стабильности
groq==0.5.0
python-dotenv==1.0.0
aiohttp==3.9.3  # Новая стабильная версия

# Дополнительные рекомендуемые зависимости
loguru==0.7.2  # Удобное логирование
uvloop==0.19.0  # Ускорение asyncio (для Linux)
async-timeout==4.0.3  # Контроль времени выполнения
